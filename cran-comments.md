## Test environments

The package was checked using GitHub Actions with the following matrix:

macOS-latest, R-release
Windows-latest, R-release
Ubuntu-latest, R-devel (HTTP user agent set to release)
Ubuntu-latest, R-release
Ubuntu-latest, R-oldrel-1


## R CMD check results

0 errors | 0 warnings | 1 note

* This is a new release.

All URLs and metadata issues noted in the previous submission have been corrected:
* Spellings are as expected, as they reference package names
